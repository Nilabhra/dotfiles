# Copy the files and directories needed to setup some common tools
# copy .zshrc .vimrc
# Setup some environment variables
# env SOME_VAR=x
# COPIED_VAR will have the same value on the remote host as it does locally
# env COPIED_VAR=_kitty_copy_env_var_

# Create some per hostname settings
# hostname nil-g5-ssm-dev
# copy env-files
# env SOMETHING=else

hostname ubuntu@35.172.121.1
copy --dest=/home/ubuntu/.zhsrc /Users/nil/.zshrc
copy --dest=/home/ubuntu/.ssh/id_ed25519 /Users/nil/.ssh/id_ed25519
copy --glob --dest=/home/ubuntu/.config/nvim/ /Users/nil/.config/nvim/
copy --glob --dest=/home/ubuntu/.aws /Users/nil/.aws
shell_integration inherited
interpreter zsh
env WANDB_API_KEY=e87a7fa1af38cce6865916584e3d82086bc0833b
env SM_USER="nilabhra.chowdhury"
env AWS_DEFAULT_PROFILE=ssm

